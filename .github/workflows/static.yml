name: publish

on:
  push:
    branches: ["github-ver"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: checkout beta
        uses: actions/checkout@v4
        with:
          ref: beta
          fetch-depth: 0

      - name: node setup
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: "npm"
      
      - name: build beta
        run: |
          git checkout beta
          npm ci
          npm run build
          mkdir -p ${{ runner.temp }}/beta
          cp -r dist/* ${{ runner.temp }}/beta/

      - name: build stable
        run: |
          git checkout main
          npm ci
          npm run build
      
      - name: prepare dist
        run: |
          mv ${{ runner.temp }}/beta dist/
          grep -rl "gitlab" dist/ | xargs sed -i \
            -e 's|sidiousious.gitlab.io/bc-addon-loader|tenjou-no-kitsune.github.io/fusam-mirror|g'

          RE_MATCH=".*@match http://localhost.*"
          TPL_MATCH="${{ runner.temp }}/match.tpl"
          cat <<EOF > "$TPL_MATCH"
          // @match https://bondageprojects.elementfx.com/*
          // @match https://www.bondageprojects.elementfx.com/*
          // @match https://bondage-europe.com/*
          // @match https://www.bondage-europe.com/*
          // @match https://bondage-asia.com/*
          // @match https://www.bondage-asia.com/*
          // @match https://bondageprojects.com/*
          // @match https://www.bondageprojects.com/*
          EOF
          grep -rl "@match http://localhost" dist/ | xargs -I {} sed -i \
            -e "\|$RE_MATCH|r $TPL_MATCH" \
            -e "\|$RE_MATCH|d" \
            {}

      - name: configure pages
        uses: actions/configure-pages@v5

      - name: upload pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: 'dist'

      - name: deploy
        id: deployment
        uses: actions/deploy-pages@v4
